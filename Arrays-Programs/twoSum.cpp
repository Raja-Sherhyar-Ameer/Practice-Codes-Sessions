#include<iostream>
using namespace std;

int twoSum(int* ptr, int size, int target);

int main(){
    
    int arr[100];
    int n;
    cout<<"Enter the size of matrix: ";
    cin>>n;
    
    // inputting the array
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }

    return 0;
}